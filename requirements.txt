# boutique_ai_project/requirements.txt

# Core Web Framework & Server
fastapi>=0.109.0,<0.112.0
uvicorn[standard]>=0.27.0,<0.30.0 # Includes websockets, httptools, etc.

# Environment & Configuration
python-dotenv>=1.0.0,<1.1.0     # For loading .env files

# HTTP Clients
requests>=2.31.0,<2.33.0         # For some SDKs or synchronous calls
aiohttp>=3.9.0,<3.10.0          # For asynchronous HTTP calls (e.g., DataWrapper to Clay webhooks)

# Telephony
twilio>=8.9.0,<9.2.0             # Check latest stable 8.x or 9.x

# Voice AI
deepgram-sdk>=3.1.4,<3.4.0       # Check latest stable

# Language Models (OpenRouter via OpenAI client)
openai>=1.13.0,<1.26.0           # For OpenRouter compatibility (OpenAI SDK v1.x)

# Database (Supabase)
supabase>=2.3.0,<2.6.0           # Check latest stable for Supabase Python client

# Caching
lru-dict>=1.2.0,<1.3.0

# Fingerprinting (Recommended for ResourceManager/BrowserAutomator)
fake-useragent>=1.5.0,<1.6.0

# Browser Automation (Core library for Playwright - install browsers separately)
# You will need to install this if you build the MultiModalPlaywrightAutomator
# playwright>=1.40.0,<1.45.0

# Utilities
# pytz # For more robust timezone handling if needed beyond basic UTC (often a dependency)
# pydantic # FastAPI uses it heavily, usually comes as a dependency. Explicit if using V2 features.

# Ensure these versions are compatible. Update based on current stable releases if needed.